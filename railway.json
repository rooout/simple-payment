{
  "build": {
    "command": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python create_packages.py && gunicorn payment_gateway.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
  }
}
