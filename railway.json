{
  "build": {
    "command": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python setup_production.py && gunicorn payment_gateway.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
